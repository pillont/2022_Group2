






git status  // => tout doit etre rouge
git add -A  // -A : all => récupérer tous les fichiers changés
git status  // => tout doit etre vert 


git commit -m "MESSAGE" //message => le tag du packet
git push origin main 	// origin : le distant / main : la branche qui va etre envoyée